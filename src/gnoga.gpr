with "aws";

project Gnoga is

   for Object_Dir use "../obj";
   for Source_Dirs use (".");

   for Library_Name use "gnoga";
   for Library_Dir use "../lib";
   for Library_Kind use "static";

   package Compiler is
      for Default_Switches ("ada") use ("-g");
      -- for Default_Switches ("ada") use ("-gnatyabcefhiklmnprst", "-gnat05");
   end Compiler;

end Gnoga;

