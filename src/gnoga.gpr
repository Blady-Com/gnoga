with "aws";

project Gnoga is

   for Object_Dir use "../obj";
   for Source_Dirs use (".");

   for Library_Name use "gnoga";
   for Library_Dir use "../lib";
   for Library_Kind use "static";

   package Compiler is
     for Default_Switches ("ada") use ("-gnatyabcefhiklmnprst", "-E");
   end Compiler;

   package IDE is
      for Documentation_Dir use "../html/gnoga_docs";
   end IDE;

end Gnoga;
